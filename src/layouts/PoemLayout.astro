---
import BaseLayout from './BaseLayout.astro';
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
  <h1 class="my-8 text-5xl font-bold">{frontmatter.title}</h1>
  <slot />
</BaseLayout>
